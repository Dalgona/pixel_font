defmodule PixelFont.Mixfile do
  use Mix.Project

  def project do
    [
      app: :pixel_font,
      version: "0.1.0",
      elixir: "~> 1.11",
      deps: deps()
    ]
  end

  defp deps do
    []
  end
end
